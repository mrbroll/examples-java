apply plugin: 'java'
apply plugin: 'application'

mainClassName = 'marvin.MultiThread'

repositories {
    flatDir {
        dirs 'libs'
    }
}

dependencies {
    compile fileTree(dir: 'marvin/framework', include: '*.jar')
}
